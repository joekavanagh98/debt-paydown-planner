# v5 - Express Backend

Work in progress. The full README will land once the version is
complete. v5 stands up an Express API with the layered architecture
(routes / controllers / services / models), Zod validation, central
error handling, pino + morgan logging, and CORS pinned to the v4
frontend. Storage is in-memory until v6 swaps in MongoDB.
